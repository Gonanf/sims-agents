# Tests automatizados — NarradorPorEventos (fork sims-agents)

Pipeline: `bun test` (package_mod.py / build_mod_real.py) + `dotnet test` (NarradorEngine xunit) + `pytest` (narrador_server.py / tests adicionales).

Ver documentación interna en PROGRESS.md — sección "Plan de tests automatizados".
